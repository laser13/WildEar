<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-files-path
        name="habitat_photos"
        path="habitat_photos/" />
    <files-path
        name="habitat_photos_internal"
        path="habitat_photos/" />

    <!-- Full recordings for FileProvider share -->
    <files-path name="recordings" path="recordings/" />

    <!-- Temporary export clips for species clip share -->
    <cache-path name="export_clips" path="export_clips/" />
</paths>
